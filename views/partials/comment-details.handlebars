<section class="comment-card">
    <div class="comment-header">
        <h6>{{author}}</h6>
        <h6>{{createdAt}}</h6>
    </div>
    <p>{{body}}</p>
</section>

{{!-- This is what every single comment looks like --}}